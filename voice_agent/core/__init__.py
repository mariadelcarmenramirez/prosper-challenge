"""Shared kernel: loop-safety guard, Pipecat runtime wiring, and prompt builders.

Depends only on the EHR tool layer; never on an architecture, so all three
architectures can build on it without depending on each other.
"""
