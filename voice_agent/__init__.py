"""Clinic appointment-scheduling voice agent.

The Pipecat bot (``bot.py``) plus the scheduling brain: a shared kernel
(``core``), the EHR tool layer (``tools``), and three interchangeable
``architectures`` selected at runtime by the ``AGENT_ARCH`` env flag.
"""
